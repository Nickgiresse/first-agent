fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20
pydantic==2.13.4
pydantic-settings==2.7.0
numpy==2.5.1

# mediapipe impose opencv-contrib-python (pas headless) comme dépendance obligatoire ;
# ne PAS ajouter opencv-python/opencv-python-headless en plus, ça créerait un conflit
# (les trois variantes s'installent sous le même module `cv2`).
mediapipe==0.10.35

# OCR : RapidOCR (moteur PP-OCRv4 en ONNX Runtime pur), fusion « best of both »
# portée du WhatsApp banking — bien plus précis que Tesseract sur photo de CNI, et
# sans binaire système à installer (modèles embarqués dans le wheel + modèle latin
# optionnel dans weights/ocr_models/). Réutilise l'onnxruntime déjà présent.
#
# ⚠️ CONFLIT OPENCV À SURVEILLER À L'INSTALL : rapidocr_onnxruntime déclare
# opencv-python, alors que mediapipe impose opencv-contrib-python (même module cv2).
# Installer rapidocr APRÈS mediapipe et, en cas de conflit cv2, réinstaller
# opencv-contrib-python en dernier (superset compatible avec les deux). Version à
# figer à l'install selon la compatibilité numpy/onnxruntime de la cible.
rapidocr_onnxruntime>=1.3,<2.0
Pillow==12.3.0

# Module 5 : inférence ArcFace (empreinte faciale) en ONNX Runtime pur, sans le paquet pip
# `insightface` (celui-ci impose opencv-python standard, en conflit avec opencv-contrib-python
# de mediapipe — même piège que EasyOCR). Modèle w600k_r50.onnx téléchargé séparément (voir README).
onnxruntime==1.28.0

pytest==8.3.4
httpx==0.28.1
