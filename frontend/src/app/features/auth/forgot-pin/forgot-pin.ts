import { Component, inject, signal } from '@angular/core';import { FormControl, ReactiveFormsModule, Validators } from '@angular/forms';import { PinService } from '../../../core/services/pin';@Component({selector:'app-forgot-pin',imports:[ReactiveFormsModule],templateUrl:'./forgot-pin.html',styleUrl:'./forgot-pin.scss'}) export class ForgotPin {private readonly pin=inject(PinService);readonly account=new FormControl('',{nonNullable:true,validators:[Validators.pattern(/^\d{23}$/),Validators.required]});readonly submitted=signal(false);readonly message=signal<string|null>(null);submit(){if(this.account.invalid)return;this.pin.requestReset(this.account.value).subscribe({next:r=>{this.submitted.set(true);this.message.set(r.data.message)},error:e=>this.message.set(e?.error?.message??'Impossible d’envoyer la demande.')})}}
